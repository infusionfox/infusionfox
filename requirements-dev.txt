# Development-only dependencies — not installed in production image.
# Install with: pip install -r requirements-dev.txt

pytest==8.3.4
pytest-cov==6.0.0
ruff==0.7.4
mypy==1.13.0
pre-commit==4.0.1

# Type stubs
types-Markdown==3.7.0.20240822
